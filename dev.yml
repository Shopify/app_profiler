name: app-profiler

type: ruby

up:
  - ruby: 3.2.0
  - bundler

commands:
  console:
    desc: "start a console"
    run: bin/console
  test:
    run: bundle exec rake test
  lint:
    run: bundle exec rubocop
